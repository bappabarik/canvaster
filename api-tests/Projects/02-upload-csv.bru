meta {
  name: 02 Upload CSV
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/projects/{{projectId}}/csv
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  csv: @file(./test-data/students.csv)
}